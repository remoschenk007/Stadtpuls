STADTPULS — GESAMTPAKET (Sweep + Züridütsch + Startsite-Sektione)
=================================================================
DRIN: index, gastro, nachtleben, nachtleben-profil, event-profil,
      events, shopping, dating, news, sp-track.js
      + setup_stats.sql (Live-Zähler ❤️/👁)
      + setup_momente.sql (Züri-Momänt-Freischaltig)

1) PUSH (alles in einem):
   cd /Users/alessandrachristen/stadtpuls && \
   unzip -o ~/Downloads/files.zip -d . && \
   git add index.html gastro.html nachtleben.html nachtleben-profil.html event-profil.html events.html shopping.html dating.html news.html sp-track.js && \
   git commit -m "Sweep komplett: Nav+Menue, Zueriduetsch 100%, Momaent+News uf de Startsite" && \
   git pull --rebase origin main && git push origin main

2) SUPABASE (SQL-Editor, je 1x Run):
   a) setup_stats.sql    -> Live-Zähler funktionieren
   b) setup_momente.sql  -> feedback kriegt status-Spalte

3) MOMÄNT FREISCHALTE (machsch du):
   select id,text from feedback where typ='moment' and status='neu';
   update feedback set status='approved' where id='<ID>';
   -> erschiint automatisch uf de Startsite (Sektion "ZÜRI MOMÄNT")

HINWEIS SEO: Meta-Descriptions (Google-Snippets) bliibed bewusst
Hochdeutsch — d Lüt sueched "Restaurants Zürich", nöd "Beizli Züri".
On-Site isch alles Züridütsch.
